import React from 'react';
import '../sass/abstracts/header.css';

const Header = () => {
	return (
		<header className="header headers header__bg">
			<h1>Browse through the selection and apply filters!</h1>
			<img src="./images/header.png" alt="" />
		</header>
	);
};

export default Header;
